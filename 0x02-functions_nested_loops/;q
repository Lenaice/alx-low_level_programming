#include "main.h"
/**
 * _islower -> checks if the character is lowercase
 * @c: a character arguement
 * Return: returns 1 and o depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
